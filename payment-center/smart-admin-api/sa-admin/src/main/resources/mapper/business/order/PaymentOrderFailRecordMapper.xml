<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.order.dao.PaymentOrderFailRecordDao">
    <select id="queryPage" resultType="net.lab1024.sa.admin.module.business.order.domain.entity.PaymentOrderFailRecordEntity">
        SELECT
            *
        FROM t_payment_order_fail_record
        WHERE id > 0
        <if test="queryForm.orderNo != null and queryForm.orderNo != ''" >
            AND order_no = #{queryForm.orderNo}
        </if>
    </select>
</mapper>